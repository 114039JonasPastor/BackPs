#spring.application.name=App

spring:
  application:
    name: App
  datasource:
    url: jdbc:postgresql://localhost:5432/Ps
    username: jonas24
    password: PracticaSupervisada123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        dll-auto: update

  # CONFIGURACIÓN DE MAIL (indentación correcta)
  mail:
    host: smtp.gmail.com
    port: 587
    username: tuoficiopracticasupervisada@gmail.com
    password: twzf lugx xoyd aswk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

#contraseña de aplicacion twzf lugx xoyd aswk

server:
  port: 8081

